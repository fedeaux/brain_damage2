require 'spec_helper'

describe BrainDamage do
  it 'has a version number' do
    expect(BrainDamage::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
